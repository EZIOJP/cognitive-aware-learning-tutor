# Optional corpus / hybrid RAG (CPU-default)
# Install: pip install -r backend/requirements-corpus.txt
rank-bm25>=0.2.2
pymupdf>=1.24.0
# Dense vectors reuse sentence-transformers from notes pipeline if installed
# Optional: faster local vector DB
qdrant-client>=1.9.0
# Optional: CPU reranker
flashrank>=0.2.0
