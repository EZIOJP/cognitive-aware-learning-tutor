# Math handwriting OCR (pix2tex / LaTeX-OCR)
# Windows + Python 3.14: full `pip install pix2tex` may fail on stringzilla (needs MSVC).
# Use the install script instead:
#   scripts\install_ocr.bat
#
# Or manually:
#   pip install pix2tex==0.1.4 --no-deps
#   pip install albumentations==1.3.1 timm==0.5.4 x-transformers==0.15.0
#   pip install torch torchvision einops munch transformers tokenizers pillow tqdm PyYAML pandas pyreadline3 opencv-python-headless

pix2tex==0.1.4
albumentations==1.3.1
timm==0.5.4
x-transformers==0.15.0
einops>=0.6.0
munch>=4.0.0
transformers>=4.18.0
tokenizers>=0.13.0
torch>=2.0.0
torchvision>=0.15.0
PyYAML>=6.0
pandas>=2.0.0
pyreadline3>=3.4.0
opencv-python-headless>=4.8.0
