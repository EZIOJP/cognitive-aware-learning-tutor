# Notes + questions + schemas (worked example)

**Purpose:** One concrete note section, matching questions, Study Loop tag stitch, and schema fields Claude must preserve.

**Out of scope:** productivity tracker / distraction gate.

Curriculum note from EdTech strategy (optional context only): CAT QA pillars weight Arithmetic and Number Systems heavily — `MT1-T02` (LCM & HCF) sits in Number Systems; keep `note_topic_ids` aligned when authoring packs. Do **not** invent a separate curriculum DB for Study Loop v1.

---

## 1. Math note section — `MT1-T02`

**File:** `data/notes/math/MT1_aptitude_interview_notes.md`

Topic Index excerpt:

```markdown
## Topic Index

- `MT1-T01` — Number systems & divisibility
- `MT1-T02` — LCM & HCF
- `MT1-T03` — Percentages
```

Section on disk:

```markdown
## `MT1-T02` — LCM & HCF

**What it is:** HCF (GCD) is the greatest shared divisor; LCM is the least shared multiple. For positive integers \(a,b\): \(\mathrm{HCF}(a,b)\times\mathrm{LCM}(a,b)=a\times b\).

**How to compute:** Prime-factorise both numbers. HCF takes the **lowest** power of each shared prime; LCM takes the **highest** power of every prime that appears.

**Classic patterns:**
- Largest number dividing \(a\) and \(b\) → HCF
- Least time/distance when events repeat → LCM
- Largest tape that cuts lengths without leftover → HCF of the lengths

**Takeaway:** Word problems are almost always “shared factor” (HCF) vs “shared schedule/multiple” (LCM).
```

**Study Loop read card** (computed, not stored):

| Field | Value |
|-------|--------|
| `card_id` | `math/MT1_aptitude_interview_notes.md::MT1-T02` |
| `tag` | `MT1-T02` |
| `title` | LCM & HCF |
| `note_path` | `math/MT1_aptitude_interview_notes.md` |
| `body_markdown` | (section body without the `##` heading) |
| `source` | `lid` |

Approach A PATCH edits **only this section**; heading id `MT1-T02` preserved; `expected_mtime` → `409` on conflict.

---

## 2. Lecture note section — `L5-T05` (trimmed)

**File:** `data/notes/L05_pandas_operations_notes.md`

```markdown
## `L5-T05` — Unique values: `unique()`, `nunique()`, `value_counts()`

**What it is:** Three levels of uniqueness questions: list values (`unique`), count (`nunique`), frequency table (`value_counts`).

**Key rule:** unique = what values exist; nunique = how many; value_counts = how often each.

**Syntax:** `series.unique()`, `series.nunique()`, `series.value_counts()`.

> **`value_counts()` always sorts descending by count** — chaining `.head(n)` gives “top N most common.”
```

**Read card id:** `L05_pandas_operations_notes.md::L5-T05`

Lecture practice today often comes from generated study decks. Study Loop Task 4 prefers reusable packs under `data/questions/mcq/` with `"note_topic_ids": ["L5-T05"]`.

---

## 3. Matching question JSON — LCM pack (`MT1-T02`)

**File:** `data/questions/math/generated/gen-lcm.json` (trimmed)

```json
{
  "schema_version": 1,
  "kind": "math",
  "topic": {
    "topic_id": "math.aptitude.gen-lcm",
    "title": "Generated LCM",
    "stage": "foundations",
    "path": ["Aptitude", "Generated", "Generated LCM"],
    "track": "aptitude",
    "prerequisites": [],
    "note_topic_ids": ["MT1-T02"],
    "description": "Procedurally generated via lukew3/mathgenerator."
  },
  "questions": [
    {
      "id": "math.aptitude.gen-lcm.q0001",
      "problem": "LCM of $12$ and $1 =$",
      "answer": "12",
      "answer_format": "expression",
      "difficulty": "easy",
      "solution_steps": [],
      "explanation": "Generated by mathgenerator id=9.",
      "hint": "Generated LCM",
      "tags": ["mathgenerator", "gen-9"]
    },
    {
      "id": "math.aptitude.gen-lcm.q0002",
      "problem": "LCM of $4$ and $14 =$",
      "answer": "28",
      "answer_format": "expression",
      "difficulty": "easy",
      "solution_steps": [],
      "explanation": "Generated by mathgenerator id=9.",
      "hint": "Generated LCM",
      "tags": ["mathgenerator", "gen-9"]
    }
  ]
}
```

**Stitch:**

1. Topic-level `note_topic_ids: ["MT1-T02"]` links the pack to the note section.  
2. Item `tags` are free tags (also appear in Study Loop tag index).  
3. `build_quiz_items(note_topic_id="MT1-T02")` returns these items.  
4. `handler.start_session(domain="math", config={"note_topic_id": "MT1-T02", "count": 5})` loads bank then **fills shortfall from math_generators**.  
5. Answers → `upsert_review_card` (FSRS).

Tag `MT1-T02` in Study Loop yields: read card + questions + due cards — **no second flashcard table**.

---

## 4. Authored closed-form — Time & work (`MT1-T07`)

```json
{
  "schema_version": 1,
  "kind": "math",
  "topic": {
    "topic_id": "math.aptitude.gen-time-work",
    "title": "Time & work (authored)",
    "note_topic_ids": ["MT1-T07"],
    "track": "aptitude"
  },
  "questions": [
    {
      "id": "math.aptitude.gen-time-work.q0001",
      "problem": "A can finish a job in 10 days. How much of the job does A do in 1 day?",
      "answer": "1/10",
      "answer_format": "expression",
      "difficulty": "easy",
      "solution_steps": ["rate = 1/days"],
      "explanation": "rate = 1/days",
      "hint": "Work rate = 1 / time.",
      "tags": ["time-work", "aptitude", "authored"]
    }
  ]
}
```

Graded with **SymPy equivalence** through the quiz handler (EdTech strategy: avoid string-match / LLM-guess keys).

---

## 5. Open / no-answer (self-check lane)

```json
{
  "id": "math.olympiad.mathnet-calculus.q0003",
  "problem": "… Prove that $\\alpha + \\beta = 90^\\circ$.",
  "answer": "",
  "answer_format": "open",
  "difficulty": "hard",
  "solution_steps": ["Let $x = \\tan \\alpha$, $y = \\tan \\beta$.", "…"],
  "explanation": "…",
  "tags": ["mathnet", "no-answer", "open"]
}
```

Empty `answer` coerces `answer_format` to `"open"`. Task 4: PATCH to fill answers; runner: reveal + Confident / Still unsure → still `upsert_review_card`.

---

## 6. Schema fields that matter

| Field | Where | Why |
|-------|--------|-----|
| `schema_version` | file | Must be `1` |
| `kind` | file | Today `"math"` \| `"coding"`; planned + `"mcq"` \| `"coding_mcq"` |
| `topic.topic_id` | topic | Stable slug |
| `topic.note_topic_ids` | topic | **Primary stitch** to `L*` / `MT*` |
| `topic.path` / `stage` / `track` | topic | Curriculum filters |
| `questions[].id` | item | Stable id for ReviewCard / catalog |
| `questions[].problem` / `answer` | math | Empty → open |
| `questions[].answer_format` | math | `number` \| `expression` \| `text` \| `open` |
| `questions[].tags` | item | Free-tag stitch |
| `questions[].test_cases` / `entry_point` | coding | `code_runner` |

Planned MCQ shape (Task 4):

```python
# McqQuestion: id, question, options[], answer_index, explanation?, hint?, tags[]
# CodingMcqQuestion: id, prompt, options[], answer_index, starter_code?, tags[]
# CONTENT_KINDS = ("math", "coding", "mcq", "coding_mcq")
```

---

## 7. Vocab stitch (planned Task 6)

| Mechanism | Rule |
|-----------|------|
| Group tag | `group_number: N` → `vocab.group.N` |
| Free tags | Word `tags[]` in Study Loop list |
| Practice | `POST /api/quiz/start` `{ "domain": "vocab", "config": { "group_number": N } }` |
| Read gate | No read card → `read_completed` auto-true |

---

## 8. End-to-end story for `MT1-T02`

```text
1. Daily Learn → Loop → pick MT1-T02
2. GET /study-loop/read-cards?tag=MT1-T02  → LCM & HCF body
3. Optional PATCH → write-back into MT1_aptitude_interview_notes.md
4. mark-read → read_completed=true
5. start-practice → handler.start_session(domain=math, note_topic_id=MT1-T02)
6. Each answer → upsert_review_card (FSRS)
7. Due tab — same SRS as everything else
```

---

*Companion: `01-COMPLETE-OVERVIEW.md` · implement: `03-STUDY-LOOP-PLAN-FOR-CLAUDE.md`*
