# Math handwriting OCR — TexTeller 3 ONNX (no pix2tex / no PyTorch)
# Full guide: docs/DEPENDENCIES.md § Tier B
#
# Python 3.14: pix2tex 0.1.4 fails (stringzilla wheel gap). TexTeller ONNX is the
# supported path — do not pin pix2tex here. Optional Tier 0: NIM_API_KEY (Nemotron vision).
#
# GPU (RTX etc.): pip uninstall onnxruntime && pip install onnxruntime-gpu
#   Requires matching CUDA/cuDNN. OCR_ONNX_DEVICE=auto (default) uses CUDA when present.
#   Force CPU: OCR_ONNX_DEVICE=cpu
#
# Windows + Python 3.14: use scripts\install_ocr.bat or ./scripts/install_ocr.sh
# First OCR request downloads ~1GB model weights from Hugging Face.

onnxruntime>=1.17
optimum[onnxruntime]>=1.24
transformers>=4.47,<5
sentencepiece>=0.2.0
opencv-python-headless>=4.8.0
# Optional UniMERNet-T second engine (scripts/install_unimernet.bat):
tokenizers>=0.15
ftfy>=6.1
