[pytest]
testpaths = tests
addopts = -m "not integration"
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated; install `httpx2` instead\.
    ignore:antlr4.error.ErrorListener module is not installed
markers =
    integration: hits production corpus index (not isolated tmp db)
