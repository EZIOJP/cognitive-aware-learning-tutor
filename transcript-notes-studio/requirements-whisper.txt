# Optional — speech-to-text (Whisper). Install when using Audio → Transcript in the GUI.
# First run downloads the model from Hugging Face (~1.5 GB for large-v3-turbo).

torch>=2.1.0
transformers>=4.46.0
accelerate>=0.34.0
datasets[audio]>=2.14.0
soundfile>=0.12.0
librosa>=0.10.0

# Lighter alternative (uncomment to use "faster-whisper" engine in GUI):
faster-whisper>=1.1.0

# Live microphone mode (Transcribe tab → Live mic):
sounddevice>=0.5.0
numpy>=1.24.0
